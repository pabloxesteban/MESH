---
description: 'Investigar un patrón de interfaz puntual (uso: /research-pattern onboarding, swipe, bottom-sheet, portfolio, tab-navigation, project-builder, match-reveal, o cualquier otro).'
---

Investigá el patrón: **$ARGUMENTS**

Tomá el rol de `ux-researcher` y producí un documento enfocado en
`docs/research/patterns/$ARGUMENTS.md` (creá el directorio si no existe) con
el formato estándar del agente.

Antes de escribir, mirá si MESH ya implementó algo relacionado —
`apps/mobile/src/design-system/`, `apps/mobile/src/features/`,
`apps/mobile/src/playground/`— y decilo en el documento: "MESH ya tiene esto
en `X`, y lo que sigue evalúa si conviene cambiarlo" o "MESH no tiene esto
todavía".

Cerrá con una recomendación de una línea sobre a quién le toca el siguiente
paso: `interaction-designer` (si es un gesto o movimiento concreto),
`ux-product-designer` (si es un flujo o copy), o "ninguno — Rechazado, sin
siguiente paso".
