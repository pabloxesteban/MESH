{
  "name": "@mesh/integration-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tests de integración contra una instancia local de Supabase",
  "scripts": {
    "integration": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mesh/domain": "*",
    "@supabase/supabase-js": "^2.112.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
